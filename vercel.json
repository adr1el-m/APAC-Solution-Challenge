{
  "version": 2,
  "buildCommand": "npm install -D tailwindcss postcss autoprefixer && npm run build",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/env.js", "destination": "/api/env.js" }
  ],
  "github": {
    "silent": true
  }
} 